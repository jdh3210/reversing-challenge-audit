# 설계 결함 감사 (AUDIT)

| | |
|---|---|
| **대상** | 자작 Windows x64 리버싱 CTF 챌린지 (`src/v1`) |
| **감사 방식** | 제작자 관점 → 공격자 관점 전환 |
| **판단 기준** | 이 요소가 공격자에게 **"맞았는지 틀렸는지 알려주는 신호(오라클)"** 를 주는가 |
| **수정 결과** | `src/v2` — REV-01 / REV-02 / REV-04 수정, REV-03 / REV-05 미해결 |
| **분석 도구** | IDA Pro (Hex-Rays) · `dumpbin` · `strings` · 자작 브루트포스 스크립트 |

각 주장은 근거 등급을 붙였다. **[사실]** = 실행·관측으로 확인, **[추론]** = 코드 해석, **[미검증]** = 추가 확인 필요.

---

## 요약

| ID | 결함 | 공격자가 얻는 것 | 심각도 | 상태 |
|---|---|---|---|---|
| [REV-04](#rev-04) | 복호화 결과를 `MZ` 시그니처로 검증 | 키 탐색 공간 256개를 즉시 확정 | **높음** | **수정** |
| [REV-01](#rev-01) | 플래그가 `printf` 인자에 평문으로 존재 | 비밀번호를 풀지 않고 플래그 획득 | **높음** | **수정** |
| [REV-02](#rev-02) | 미끼 비밀번호가 가짜 플래그를 출력 | 플래그 포맷 확인 · 미끼 경로 오인 | 중간 | **수정** |
| [REV-03](#rev-03) | 복호화 키가 코드 무결성에 묶여 있지 않음 | 안티디버깅 무력화 | 중간 | *미해결* |
| [REV-05](#rev-05) | 복호화 이후 DLL이 RWX 메모리에 평문 상주 | 메모리 덤프로 DLL 획득 | 중간 | *미해결* |

**우선순위 기준:** 심각도가 아니라 *"의도한 풀이 경로를 얼마나 무의미하게 만드는가"* 로 정렬했다. REV-04를 1순위로 둔 이유는 그것 하나로 챌린지 1단계 전체가 분석 대상에서 제외되기 때문이다.

---

<a id="rev-04"></a>
## REV-04 — 무결성 검사가 브루트포스 오라클로 전락

### 결함

복호화가 제대로 됐는지 확인하려고 둔 `MZ` 시그니처 검사가, 공격자에게 **XOR 키 전수조사의 정답 판별기**를 그대로 제공한다.

**위치:** `src/v1/main.c` — TLS Callback (`sub_140002000`) 내부, 셸코드 복호화 직후

```c
if (shellcode[0] == 0x4D && shellcode[1] == 0x5A) {   // 'M','Z'
    sub_140001000(shellcode);        // Reflective Loader
} else {
    return;
}
```

### 공격 시나리오 [사실]

XOR 키가 1바이트이므로 후보는 256개뿐이다. "복호화 결과가 `MZ`로 시작하는가"는 그 256개 중 정답 하나를 고르는 **완전한 판별식**이다.

```python
# src/v1/brute.py
data = open("shellcode.bin", "rb").read()
for key in range(256):
    dec = bytes(b ^ key for b in data)
    if dec[0:2] == b"MZ":
        print(f"[+] key found: 0x{key:02X}")
        break
else:
    print("[-] no MZ oracle: key not found")
```

`shellcode` 배열은 IDA에서 덤프하거나 `extract.py`로 헤더에서 추출한다. 실행 결과 v1에서 **키 `0xA1`이 즉시 확정**됐다.

### 영향

의도한 풀이 경로는 "TLS 콜백을 읽고 `PEB.BeingDebugged` · 파일 크기 · 팀명 체크섬 세 재료를 역산해 키를 유도한다"였다. 그런데 판별 근거가 있으면 **키 유도 로직을 전혀 분석하지 않아도 된다.**

> 공들여 설계한 3중 키 유도 구조가 무결성 검사 두 줄 때문에 분석 대상에서 통째로 제외됐다. 방어 기제를 추가했는데 난이도가 내려간 사례다.

심각도를 가장 높게 둔 이유이며, **REV-01(평문 플래그)보다 위험하다고 판단한 근거**다. REV-01은 플래그 하나를 잃지만, REV-04는 챌린지의 1단계 설계 전체를 무의미하게 만든다.

### 수정 (v2)

판별 근거가 되는 상수를 **XOR 암호화 이전에** 제거하고(헤더 스터빙), 검사 코드 자체를 삭제했다.

**`src/v2/encoder_v2.py`** — `dll_data`를 읽은 직후, 암호화 전:

| 제거 대상 | 위치 | 제거 이유 |
|---|---|---|
| `MZ` (`e_magic`) | `0x00` | 직접적인 판별 근거 |
| `PE\0\0` (`Signature`) | `e_lfanew` | 동일한 역할의 2차 판별 근거 |
| DOS 스텁 전체 | `0x40` ~ `e_lfanew` | `This program cannot be run in DOS mode` 문자열이 MZ와 동일한 판별 근거 |

**`src/v2/main_v2.c`** — `if (shellcode[0] == 0x4D && ...)` 블록 제거, `ReflectiveLoader(shellcode)` 직접 호출.

`ReflectiveLoader`는 `e_lfanew`만 참조하고 `e_magic`·`Signature`를 검사하지 않으므로 **시그니처 복원 없이 그대로 로드된다.** 기능 손실 없이 오라클만 제거되는 구조다. [사실] — 빌드 후 실행으로 확인

### 수정 후 검증 [사실]

| 증거 | v1 | v2 |
|---|---|---|
| `python brute.py` | `[+] key found: 0xA1` | `[-] no MZ oracle: key not found` |
| 스크린샷 | `img/02a_brute_v1.png` | `img/02b_brute_v2.png` |

복호화된 v2 셸코드를 직접 확인한 결과 첫 2바이트가 `00 00`, `e_lfanew` 위치의 시그니처도 `00 00 00 00`, `0x40`~`e_lfanew` 구간 전체가 `00`이었다. `e_lfanew`(오프셋 `0x3C`)는 보존되어 로더가 정상 동작한다.

### 잔여 위험 [추론]

**브루트포스 자체는 여전히 가능하다.** 섹션 이름(`.text`, `.rdata`), import DLL 이름(`KERNEL32.dll`), 함수 이름 문자열이 복호화 결과에 남아 동일한 판별 근거로 쓰일 수 있다. 공격 비용을 올렸을 뿐 차단하지 못했다. 근본 대응은 REV-03과 함께 다뤄야 한다.

---

<a id="rev-01"></a>
## REV-01 — 보호 대상을 정반대로 선택

### 결함

**정답 비밀번호는 XOR로 난독화**해 배열에 넣어두고, **플래그는 `printf` 인자에 평문 문자열로** 두었다.

**위치:** `src/v1/dllmain.cpp` — `my_strcmp()`

```c
unsigned char encrypted_flag[] = { 0xEC, 0x8D, 0xF8, ... };   // 실제로는 '비밀번호'
for (int i = 0; i < flag_len; i++)
    real_flag[i] = encrypted_flag[i] ^ 0xBE;                  // -> R3FL3CTIV3_MAST3R

if (original_strcmp(str1, real_flag) == 0) {
    printf("You've got it! Real Flag is DH{r3fl3ct1v3_m4st3r}\n");   // 평문 플래그
}
```

변수명이 `encrypted_flag` / `real_flag`인 것 자체가 당시의 혼동을 보여준다. 실제로 그 배열에 들어 있는 것은 플래그가 아니라 비밀번호다.

### 공격 시나리오 [사실]

셸코드를 복호화해 DLL을 얻은 뒤 문자열 검색 한 번으로 끝난다.

```
strings RealDll.dll | findstr "DH{"
You've got it! Real Flag is DH{r3fl3ct1v3_m4st3r}
```

비밀번호를 역산할 필요도, `^ 0xBE`를 알아낼 필요도 없다.

### 영향

> 비밀번호는 **맞히면 되는 값**이고, 플래그는 **맞힌 사람만 받아야 하는 값**이다. v1은 전자를 숨기고 후자를 노출했다. 보호 대상을 정확히 반대로 고른 것이다.

### 수정 (v2)

플래그를 **정답 입력에서 유도한 키스트림**으로만 복호화할 수 있게 바꿨다. 오답을 넣으면 복호화 결과가 쓰레기이고 해시 검증에서 걸러지므로, **플래그는 바이너리에도 프로세스 메모리에도 존재하지 않는다.**

**`src/v2/flag_gen.py`** (신규) — 빌드 파이프라인의 최초 단계. 정답 비밀번호로 플래그를 암호화해 `flag_blob.h`(`enc_flag[]` + `FLAG_HASH`)를 생성한다.

**`src/v2/dllmain_v2.cpp`** — `my_strcmp()` 내부:

```c
seed = fnv1a64(str1, in_len);            // 사용자 입력이 키의 유일한 출처
keystream(seed, ks, sizeof(enc_flag));
out[i] = enc_flag[i] ^ ks[i];

if (fnv1a64(out, sizeof(enc_flag)) == FLAG_HASH)   // '복호화 결과'를 검증
    printf("You've got it! %s\n", (char*)out);

SecureZeroMemory(out, sizeof(out));      // 실패 경로에서 즉시 파기
SecureZeroMemory(ks, sizeof(ks));
```

**설계 판단 두 가지:**

1. **검증 대상을 입력이 아니라 복호화 결과로 뒀다.** 입력의 해시를 검증하면 그 해시가 곧 비밀번호 판별 오라클이 된다(REV-04와 같은 실수의 반복). 복호화 결과를 검증하면, 해시 충돌로 통과하더라도 출력되는 것은 플래그가 아니므로 공격자에게 유효한 정보가 남지 않는다.
2. **해시를 바이너리에 두는 것이 오프라인 공격을 열어주지 않는가** — 해시는 판정용이고 플래그 보호는 입력 유도 키가 담당한다. 해시를 깨서 정답을 찾았다면 그것은 곧 문제를 푼 것이다.

Python 생성기와 C 복호화기는 바이트 단위로 동일한 `fnv1a64`·`keystream`을 구현한다(아래 명세).

### 수정 후 검증 [사실]

| 증거 | v1 | v2 |
|---|---|---|
| `strings RealDll.dll \| findstr "DH{"` | 플래그 노출 | 출력 없음 |
| 스크린샷 | `img/04a_strings_v1.png` | `img/04b_strings_v2.png` |
| IDA `my_strcmp` 슈도코드 | `v4[] ^ 0xBE` + `printf` 인자에 평문 | `fnv1a64` → `keystream` → 해시 `0x85E7BEFF532BC1C8` 비교, 출력은 `%s` |
| 스크린샷 | `img/03a_ida_v1.png` | `img/03b_ida_v2.png` |
| 정답 입력 시 출력 | `Real Flag is DH{...}` (평문 상수) | `You've got it! DH{...}` (`%s`, 런타임 복호화 결과) |

암호화된 셸코드 자체를 검색해도 v2에서는 플래그 문자열이 나오지 않는다. [사실]

### 잔여 위험

정답 입력 시점에는 복호화된 플래그가 스택에 존재한다. 출력 후 `ExitProcess`로 즉시 종료되고 실패 경로에서는 `SecureZeroMemory`로 파기하지만, **정답을 아는 사람이 그 순간을 덤프하는 것은 막지 못한다.** 다만 그 조건은 이미 문제를 푼 상태이므로 실질적 위험이 아니다. [추론]

---

<a id="rev-02"></a>
## REV-02 — 미끼가 오라클로 기능

### 결함

미끼 비밀번호가 가짜 플래그를 출력하여 **플래그 포맷을 노출**하고, 풀이자가 미끼 경로를 정답 경로로 오인하게 만든다.

**위치:** `src/v1/main.c` — `main()`

```c
if (strcmp(input, "EASY_REVERSING") == 0) {
    printf("Wow, Are you genius? flag is DH{too_easy_password}\n");
    continue;
}
```

### 공격 시나리오 [사실]

`strings`로 `EASY_REVERSING`을 발견해 입력하면 `DH{too_easy_password}`가 출력된다. 이로써 **플래그 포맷이 `DH{...}`임이 확정**되고, 이후 `DH{` 문자열 검색만으로 진짜 플래그를 찾을 수 있다(REV-01과 결합해 위험이 증폭된다).

### 수정 (v2)

미끼 문자열은 **단서로서 `.rdata`에 그대로 남기고**, 비교만 실패하게 만들었다. 이미 설치한 IAT 후킹을 두 번째 목적으로 재사용하는 구조다.

**`src/v2/dllmain_v2.cpp`** — `my_strcmp()` 진입부:

```c
if (original_strcmp(str2, "EASY_REVERSING") == 0)
    return 1;          // 미끼 비교를 항상 불일치로 만든다
```

`main()` 쪽 코드와 문자열은 **건드리지 않았다.** 풀이자는 여전히 미끼를 발견하고 시도할 수 있지만 `Wrong~`으로 떨어진다. **단서는 유지하고 보상만 제거**한 것이다.

### 수정 후 검증 [사실]

| 증거 | v1 | v2 |
|---|---|---|
| `EASY_REVERSING` 입력 | `Wow, Are you genius? flag is DH{too_easy_password}` | `Wrong~` |
| 스크린샷 | `img/05a_run_decoy_v1.png` | `img/05b_run_decoy_v2.png` |

IDA 슈도코드에서도 `my_strcmp` 진입부의 `return 1` 분기를 확인할 수 있다(`img/03b_ida_v2.png`).

### 잔여 위험 [추론]

`DH{too_easy_password}` 문자열은 `main()`에 그대로 남아 있으므로 **정적 분석으로는 여전히 플래그 포맷을 알 수 있다.** 실행 경로만 차단한 것이며, 문자열 제거까지 하면 단서로서의 기능도 사라지므로 의도적으로 남겼다.

---

<a id="rev-03"></a>
## REV-03 — 복호화 키가 코드 무결성에 묶여 있지 않음 *(미해결)*

### 결함

셸코드 XOR 키는 세 재료에서 유도된다.

| 키 | 출처 | 역할 |
|---|---|---|
| `key1` | `0x77 * (1 - PEB.BeingDebugged)` | 안티디버깅을 분기가 아니라 키 재료로 편입 |
| `key2` | `(shellcode_len >> 8) & 0xFF` | 페이로드 크기 바인딩 |
| `key3` | 팀명 문자열 바이트 합 `& 0xFF` | 정적 상수 은닉 |

안티디버깅을 별도 분기가 아니라 키 재료에 섞은 것은 의도적인 설계다 — "디버깅을 감지하면 종료한다"가 아니라 "디버깅 중이면 애초에 동작하지 않는다"가 되기 때문이다. 그러나 **어떤 재료도 코드 자체의 무결성에 묶여 있지 않다.**

### 공격 시나리오 [추론]

- `PEB.BeingDebugged`를 0으로 패치하면 디버거를 붙인 채로도 `key1`이 복원된다.
- 또는 `key1 = 0x77 * (1 - ...)` 계산을 `key1 = 0x77` 상수로 패치한다.
- 어느 쪽이든 **코드를 변조해도 복호화는 성공**하므로 안티디버깅이 무력화된다.

### 수정 방안 (향후)

`.text` 섹션의 체크섬을 키 재료에 결합해, **코드가 변조되면 체크섬이 달라져 복호화가 실패**하게 만든다.

```
final_key = key1 ^ key2 ^ key3 ^ checksum(.text)
```

이렇게 하면 안티디버깅 분기를 패치하는 순간 `.text`가 바뀌어 셸코드가 깨진다. 패치와 복호화 성공을 동시에 달성할 수 없게 되는 구조다.

### 미해결로 둔 이유

체크섬 계산 범위와 빌드 재현성 확보에 추가 작업이 필요하고(링커 배치·재배치 처리), 이번 수정 범위를 "오라클 제거"로 한정했기 때문이다. **키가 원리적으로 비밀이 될 수 없다는 한계**는 아래 위협 모델에 함께 적었다.

---

<a id="rev-05"></a>
## REV-05 — 복호화 이후 메모리 상주 *(미해결)*

### 결함

TLS 콜백은 `shellcode[]`를 **in-place로 복호화**하고 `VirtualProtect`로 RWX 권한을 부여한다. 복호화가 끝난 뒤에도 원본 버퍼가 평문 상태로 프로세스 메모리에 남는다.

### 공격 시나리오 [추론]

1. TLS 콜백이 끝난 뒤(= `main`의 입력 대기 중) 프로세스에 디버거를 어태치한다. 이 시점에는 이미 복호화가 끝났으므로 `PEB.BeingDebugged` 검사를 우회한 것과 같다.
2. 메모리를 덤프해 DLL 이미지를 그대로 획득한다.
3. `ReflectiveLoader`가 매핑한 새 이미지(RWX로 `VirtualAlloc`)도 별도로 덤프 가능하다.

### 수정 방안 (향후)

- `ReflectiveLoader`가 로드를 완료한 직후 원본 `shellcode[]` 버퍼를 `SecureZeroMemory`로 파기한다.
- 매핑된 이미지의 헤더를 다시 스터빙해 덤프 결과의 복구 비용을 올린다.
- 페이지 권한을 필요 최소로 되돌린다(RWX → RX).

### 부분적 완화 [추론]

REV-04의 헤더 스터빙 덕분에 **덤프 결과는 시그니처가 손상된 PE**다. 그대로는 로드·분석 도구에 올라가지 않으므로 `MZ`·`PE\0\0`·DOS 스텁을 복원하는 작업이 추가된다. 차단은 아니고 비용 증가다.

---

## 부록 — v2 암복호 알고리즘 명세

생성기(Python)와 복호화기(C)가 **바이트 단위로 동일**해야 하므로 명세를 고정했다.

| 항목 | 정의 |
|---|---|
| 해시 | FNV-1a 64bit — offset `0xCBF29CE484222325`, prime `0x100000001B3`, 2^64 랩어라운드 |
| 키스트림 | `h = h * PRIME; h ^= (h >> 29)` 반복, 매 라운드 **8바이트 리틀엔디안**으로 출력. 마지막 블록은 하위 바이트부터 절단 |
| 시드 | `fnv1a64(사용자 입력 바이트열)` |
| 암호문 | `enc_flag[i] = flag[i] XOR ks[i]` |
| 판정 | `fnv1a64(복호화 결과) == FLAG_HASH` |
| 실패 처리 | `SecureZeroMemory`로 복호화 버퍼·키스트림 즉시 파기 (컴파일러 최적화로 제거되지 않음) |

구현 위치: `src/v2/flag_gen.py` (생성) · `src/v2/dllmain_v2.cpp` (복호화)

---

## 위협 모델과 한계

본 챌린지는 **공격자가 로컬에서 바이너리를 완전히 통제하는 CTF 환경**을 전제한다. 이 조건에서 완전한 anti-reversing은 성립하지 않는다. 감추지 않고 명시한다.

1. **브루트포스 자체는 여전히 가능하다.** MZ 오라클은 제거했으나 섹션 이름·import DLL 이름·함수 이름 문자열이 복호화 결과에 남아 동일한 판별 근거로 쓰일 수 있다. 공격 비용을 올렸을 뿐 차단하지 못했다. [추론]

2. **셸코드 복호화 키는 원리적으로 비밀이 될 수 없다.** 프로그램이 사용자 입력 없이 스스로 키를 유도해야 하므로, 어떤 방식으로 구성하든 정적 분석으로 추적 가능하다. REV-03의 `.text` 체크섬 바인딩은 *변조를 탐지*할 수는 있어도 *키를 숨기지는* 못한다.

3. **REV-05(메모리 상주)는 미해결이다.** 위 시나리오대로 덤프가 가능하며, 헤더 스터빙으로 인한 비용 증가만 있다.

4. **v2의 목표는 절대적 보호가 아니다.** *의도한 풀이 경로를 무의미하게 만드는 설계 결함의 제거*다. 그 기준에서 REV-01·REV-02·REV-04는 목표를 달성했고, REV-03·REV-05는 남았다.

### 이 감사에서 얻은 결론

가장 위험했던 것은 암호 강도나 난독화 수준이 아니라 **"맞았는지 알려주는 신호"의 존재**였다.

- REV-04는 *무결성 확인용으로 넣은 코드*가 공격자의 판별기가 된 경우다.
- REV-02는 *단서로 넣은 미끼*가 플래그 포맷 오라클이 된 경우다.
- REV-01의 수정에서도 같은 함정을 한 번 더 피해야 했다 — 입력 해시를 검증하면 그 해시가 또 오라클이 되므로, 검증 대상을 복호화 결과로 옮겼다.

**방어 기제를 추가할 때마다 "이것이 공격자에게 무엇을 알려주는가"를 함께 물어야 한다.** 이 감사의 핵심 교훈이다.
